# A* is a graph traversal and path search algorithm, which is an extension of Dijkstra's algorithm, except it 
# uses a heuristic function to estimate the total cost to reach the destination node from the current node.

